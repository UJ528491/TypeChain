{
  // node.js를 평범하게 사용하고 다양한 걸 import 혹은 export 할 수 있게 만듬
  "compilerOptions": {
    "module" : "CommonJS",
    "target": "ES2015", //어떤 버전으로 컴파일 될지
    "sourceMap": true,
  },
  //어떤 파일들이 컴파일 과정에 포함되는지 typescript에게 알려줌
  "include": ["index.ts"],
  "exclude": ["node_modules"]
}